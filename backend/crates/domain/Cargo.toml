[package]
name = "domain"
version = "0.1.0"
edition = "2021"

[lib]
name = "domain"
path = "src/lib.rs"

[dependencies]
uuid = { version = "1.10", features = ["v4"] }
async-trait = "0.1"
chrono = "0.4"